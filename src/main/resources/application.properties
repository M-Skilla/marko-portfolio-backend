spring.application.name=marko-portfolio

# H2 Database - file-based, data persists across server restarts
#spring.datasource.url=jdbc:h2:file:./data/marko-portfolio;DB_CLOSE_ON_EXIT=FALSE
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

# PostgreSQL database
spring.datasource.url=jdbc:postgresql://localhost:5432/portfolio
spring.datasource.username=marko
spring.datasource.password=2323
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

## Keep the existing schema/tables across restarts and create them if missing
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/portfoliodb
spring.datasource.username=marko
spring.datasource.password=2323
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Optional: enable the H2 web console at http://localhost:8080/h2-console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

jwt.secret=${JWT_SECRET}
jwt.expiration-ms=${JWT_EXP_MS}
